---
store:
  mode: READ_Write
  mode: khj
server:
  externalUrl: ${EXTERNAL_URL:-https://demo.ldproxy.net}
backgroundTasks:
  maxThreads: 8
logging:
  level: OFF
  sqlQueries: OFF
  sqlResults: OFF
  stackTraces: ON
  appenders:
    - type: console
      timeZone: Europe/Berlin
  loggers:
    de.ii: INFO
